# Metronome

Closes [#7](https://github.com/benstreich/etude/issues/7). Reached from the **Metronome** pill on the
Practice picker and on the running-session screen — both open the same sheet, and the metronome keeps
running when the sheet closes.

## What it does

| | |
|---|---|
| Tempo | 20–300 BPM. −5 / −1 / +1 / +5 buttons and tap tempo (averages the last 5 taps; a gap over 3s starts a new count-in) |
| Time signature | 1/4–5/4, 6/8, 7/8, 9/8, 12/8. Beat 1 gets the accented click; compound meters (6/8, 9/8, 12/8) add a lighter click on each group of three. 1/4 means no accent; 7/8 stays downbeat-only since its grouping varies by piece |
| Accent pattern | Tap a beat dot to cycle it: accent → group → plain → **muted**. Muting is the point of it — practising against a click that drops beats 1 and 3 is a different exercise. Changing the signature resets the bar, because an accent on beat 5 means nothing in 3/4 |
| Subdivision | Off · 2 · 3 · 4 clicks per beat, on a quieter sample so the pulse still reads. Labelled "N per beat" rather than by note name, since in 6/8 a beat is already an eighth |
| Click sound | Five sets — Wood (the original), Click, Beep, Soft, Rim — each four samples deep (plain / group / accent / subdivision). Tapping a set previews it while the metronome is stopped |
| Click volume | 0–100% under the system volume, so the click can sit beneath a recording |
| Tempo ramp | "+N BPM every M bars or seconds, until T". A target **below** the current tempo ramps down instead — the direction comes from the target, so there is no sign to get wrong |
| Outside the app | Keeps clicking with the app backgrounded or the screen locked, and the lock screen / notification carries slower · play-pause · faster buttons (±5 BPM) |

The saved tempo is what a run *starts* from; a ramp moves the live tempo only, and stopping snaps back
to the saved one. Setting the tempo by hand mid-run rebases the ramp from there and leaves the saved
tempo alone — so nudging from the lock screen during a ramp doesn't quietly rewrite your setting.

## Where the code is

| File | What |
|---|---|
| `src/lib/metronome-math.ts` | Pure arithmetic: `bpmAfter`, `describeRamp`, `tapTempo`, clamping. No React, no Expo |
| `src/lib/metronome.tsx` | `MetronomeProvider` / `useMetronome` / `useBeat` — scheduler, click pool, lock-screen wiring |
| `src/components/metronome.tsx` | `MetronomeButton` (pill + sheet) |
| `modules/metronome-controls/` | Local Expo module: the lock-screen transport |
| `assets/audio/<set>_<level>.wav` | 5 sets × 4 samples (`beat` / `mid` / `accent` / `sub`), generated by `scripts/make-click.py`, which also copies them into the Android module's `res/raw` |
| `scripts/check-metronome.ts` | `npm run check:metronome` — asserts the ramp math |

Settings live flat on the store (`metroBpm`, `metroTimeSig`, `metroRamp*`, `metroSubdiv`,
`metroAccents`, `metroSound`, `metroVolume`) so the shallow
seed-merge in `store.tsx` backfills each key on its own for installs that predate them. The ramp unit
was once four values; anything that isn't `seconds` now counts bars, so a `'beats'` or `'minutes'`
left in an old install degrades quietly instead of needing a migration. Likewise a
`metroBeatsPerBar` saved by the pre-signature build is mapped to `N/4` on load.

## Levels, and why there are four

One scale is used everywhere a click is decided — `metronome-math.ts`, the scheduler, the Kotlin
service, the stored pattern: **0 muted · 1 plain · 2 group start · 3 bar downbeat**. The subdivision
click is *not* a level; it is a fourth sample played between the beats. Every sample set lists its
four in that order (`beat`, `mid`, `accent`, `sub`), the JS pool indexes them that way and so does
`SOUND_SETS` in the Kotlin module — which is the only reason a set can be added on one side without
the other noticing. Add a set in `make-click.py`, `SOUND_SETS` (math), `SAMPLES` (JS) and
`SOUND_SETS` (Kotlin), in that order.

`metroAccents` is stored empty until the user edits a bar, so a fresh install and an old one both
just take whatever the signature implies. `fitAccents` cuts or tops up a saved bar against the
current signature, which is also what protects the app from a pattern left by another meter.

## How the beat is scheduled

**Android** (a dev build): the clicks are not scheduled at all — they are *mixed*. The native
`Ticker` (`modules/metronome-controls`, Kotlin) feeds one `AudioTrack` a continuous mono stream in
10 ms chunks and writes each click's PCM at the exact sample offset where the beat falls. A frame in a
stream cannot be late or early, so the grid holds to the sample whatever the JS thread is doing, and
every sound set behaves the same — the 22 ms rim sample no longer depends on a fresh SoundPool stream
spinning up in time. This engine is the only click source whenever the metronome runs, in the app or
with the screen off; there is no handover any more. It reports each tick to JS (`onTick`) for the beat
dots and the ramp, delayed by what is queued ahead of the speaker so the dot moves when the click is heard.

The bar lives natively as `beatFrame` (the current downbeat's frame) plus `beat`/`sub`, the next tick.
Subdivision ticks sit on the beat's own grid (`beatFrame + sub · beatLen / n`), so changing the
subdivision mid-beat re-slices the current beat rather than limping, and a tempo edit moves the pending
click at once. Tempo, accent pattern, subdivision, sound and volume are `@Volatile` fields read as each
tick is placed; JS pushes edits down with `updateTicking`, and the ramp's tempo with `update`.

**Elsewhere** (iOS, Expo Go, web): a `setTimeout` chain that re-aims at the wall clock every tick.
With subdivisions on it fires per *subdivision* (`60000 / bpm / subdiv`) and counts `{beats, sub}`
rather than a tick index — `advanceTick` — so changing the subdivision mid-run lands on the next beat
instead of re-slicing the bar. The ramp only ever sees whole beats, and the tempo for the *next*
interval is recomputed from elapsed counters rather than accumulated per beat, so a dropped tick can't
drift it. `nextTick` decides where the successor of a tick goes: a tick the JS thread let fire late
used to get its successor scheduled `interval` after the *planned* time, which at 120 BPM and 300 ms
late is two clicks 200 ms apart — the double click. Now a successor whose slot is more than a quarter
gone is dropped and counted (the bar keeps its phase), and a gap over 500 ms means the process was
suspended, so the grid restarts from now instead of firing a burst of catch-up clicks.

The JS clock is only as punctual as the JS thread, a few ms when idle. Clicks there come from two
expo-audio players per sound, used alternately; each is rewound 150 ms after it fires, so the beat
itself is a bare `play()`.

## The lock-screen module

`modules/metronome-controls` is a local Expo module (autolinked from `modules/`, no `package.json`
entry needed). It exposes `show` / `update` / `hide` and emits `onCommand` with `inc` | `dec` |
`toggle`; JS decides that those mean ±5 BPM. `startTicking` / `updateTicking` / `stopTicking` drive
the beat engine described above: tempo, accent pattern, subdivision, sound set and volume. The engine
does not depend on the service — it starts on the JS call, while `startForegroundService()` is still
creating the service — so a start from the lock screen clicks at once too. The service's job is the
notification and keeping the process alive with the screen off.

- **iOS** — `MPRemoteCommandCenter` (⏭ → faster, ⏮ → slower, play/pause → toggle) plus
  `MPNowPlayingInfoCenter` showing "96 BPM". It deliberately replaces expo-audio's
  `player.setActiveForLockScreen()`: both write the same now-playing info, so only one may be in charge.
- **Android** — a `mediaPlayback` foreground service whose ongoing notification carries the three
  buttons. The service also keeps the process alive with the screen off. `update()` goes through a
  static reference to the running service rather than an Intent, because a backgrounded app may not
  legally call `startService()` on API 26+.
- **Web** — a no-op stub, so callers need no platform checks.

It is loaded with `requireOptionalNativeModule`, so in Expo Go — where the native half isn't built —
the module is `null`, every call is skipped and the metronome still works in-app.

### Not verified

The native halves have never been compiled: this repo has no `android/`/`ios/` (CNG), and no dev
build exists yet. **Everything above the native boundary is verified** — ramp up, ramp down, target
clamping, stop-resets-tempo and the UI were all driven in a browser against the real app. The Kotlin
and Swift are unproven until someone runs:

```
npx expo prebuild --clean
npx expo run:android        # or: npx eas build --profile development --platform android
```

Expect the usual first-build fixes there. On Android 13+ the notification only appears once
`POST_NOTIFICATIONS` is granted; the metronome asks for it (via expo-audio's
`requestNotificationPermissionsAsync`) the first time it starts.

### Known ceilings

- The Android notification is a plain ongoing notification, not a `MediaStyle` one — it shows the
  buttons but not the compact media layout. Making it media-styled means owning a `MediaSession`.
- `interruptionMode: 'doNotMix'` is required for the lock screen to be ours, so starting the
  metronome will duck/stop other apps' audio.
- Recording sets the audio mode too; `practice.tsx` passes `shouldPlayInBackground: true` there so
  starting a recording can't cut off a metronome that is already running in the background.
